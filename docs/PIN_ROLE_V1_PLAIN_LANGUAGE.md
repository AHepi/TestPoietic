# PIN-ROLE-v1 in Plain Language

record: PIN-ROLE-v1 (official file: PIN_ROLE_V1.md)
status: DRAFT_PENDING_REVIEW — orchestrator hand-review only; no
independent reviewer was available

## What this is

Seven more undefined words get proposed meanings, in three groups. For
N17 (a higher-level explanation is not an extra thing): RoleEq means the
higher-level account and its physical realization answer the same task;
SecondSubstance means the structure posits an extra entity; and
CausalExemption means the higher-level account is declared exempt from
physical causation. For N19 (one copy is not a counterfactual role):
OneObservedToken counts exactly one observed token;
CounterfactualFamilyObserved means a family of nearby variants with
observed outcomes exists in the evidence; ReplicationRole means the pair's
role survives across a declared family of variants in its environment
class. And RetainsOrAdapts — left open by the boundary-move record because
either natural reading rigged row N20 — now gets a safe reading: a
variant's role profile (the machinery from the N18 record) is preserved
or adapted in the new environment. Because role profiles are free per
pair, this rigs nothing.

## Why the readings are shaped this way

Each row must remain winnable and losable. N17 needs RoleEq true with no
extra substance and no exemption — the emergence picture the source
register allows. N19 needs one copy without any observed variant family
or role. N20 needs an outside-the-suite pair whose retention can go either
way. The record exhibits both outcomes for each row's limb under the
pins, and records the rejected readings (identity of levels, same-output
equality, copy-equals-role) with their reasons.

## Honest dependencies

This record reuses two earlier drafts that you have not sealed yet —
PIN-CONS-v1 (which had left RetainsOrAdapts open) and PIN-SUB-v1 (whose
role machinery the new reading uses). The record says so on its face: if
either is amended, this record must be re-checked. The orchestrator
reviewed this record by hand; no independent reviewer exists right now,
and the manifest says that too.

## What did NOT change

No N-row was tested or discharged. Counts stand: 0 pinned, 2 partial, 18
open; zero discharged; testing PROHIBITED. The calculus, SPA-v1, and all
sealed records are unedited. Nothing was imported from the source
literature as a theorem — the register's constraints shaped the readings
only.

## What happens next

The epistemic cluster (Pass, Possible, Artifact, Record, Deduction,
Prediction, Confirmed, CreativeGenerator, NoPossibleCritic, FinalOutput),
then the bridge review — the last item in the frozen order.
